import Countryitem from "./CountryItem";

function Main({ themeStyles }) {
    return (
        <main>
            <Countryitem themeStyles={themeStyles} />
        </main>
    );
}

export default Main;
